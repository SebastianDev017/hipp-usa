{% comment %}
---------------------------------------------------------------------
EXTERNAL URLS / APPS
---------------------------------------------------------------------
{% endcomment %}

<link rel="preconnect" href="//cdn.shopify.com" crossorigin>
<!-- <link rel="preconnect" href="//static.klaviyo.com" crossorigin> -->
<!-- <link rel="preconnect" href="//fast.a.klaviyo.com" crossorigin> -->
{%- comment -%}
  El preconnect a Judge.me se quitó: la app no está instalada en esta tienda, así
  que abría un handshake DNS+TLS inútil en cada carga. Si se instala, la app
  inyecta lo suyo.
{%- endcomment -%}
<!-- <link rel="preconnect" href="https://services.mybcapps.com" crossorigin> -->

{% comment %}
---------------------------------------------------------------------
FUENTES
---------------------------------------------------------------------
{% endcomment %}
{%- comment -%}
  Solo las dos que aparecen sobre el pliegue: el texto de la pagina y los
  encabezados. Semibold y Bold cargan cuando hacen falta.

  Estas URLs salen del MISMO asset_url que los @font-face de
  snippets/hipp-fonts.liquid, asi que coinciden exacto y no hay doble descarga.
  Ojo: css-base.css declara las mismas fuentes con otra query string; si se
  precargaran esas, el navegador bajaria cada archivo dos veces.
{%- endcomment -%}
<link rel="preload" href="{{- 'BananaGrotesk-Medium.woff2' | asset_url -}}" as="font" type="font/woff2" crossorigin>
<link rel="preload" href="{{- 'AdvertBold.woff2' | asset_url -}}" as="font" type="font/woff2" crossorigin>

{% comment %}
---------------------------------------------------------------------
CSS
---------------------------------------------------------------------
{% endcomment %}

<link rel="preload" href="{{ 'theme.css' | asset_url }}" as="style">
<link rel="preload" href="{{ 'css-base.css' | asset_url }}" as="style">
<link rel="preload" href="{{ 'theme.js' | asset_url }}" as="script">
<link rel="preload" href="{{ 'js-commons.js' | asset_url }}" as="script">
<link rel="preload" href="{{ 'js-base.js' | asset_url }}" as="script">


{%- comment -%}
  La coleccion ya no precarga css-main-collection.css ni js-main-collection.js:
  la seccion nueva no los usa y precargar un asset que nadie consume tira el
  warning "preloaded but not used" y compite por ancho de banda con el LCP.
{%- endcomment -%}

{% if template.name == "product" %}
  <link rel="preload" href="{{ 'hipp-product.js' | asset_url }}" as="script">
{% endif %}

{% if template.name == "cart" %}
  <link rel="preload" href="{{ 'theme-cart.css' | asset_url }}" as="style">
  <link rel="preload" href="{{ 'css-main-cart.css' | asset_url }}" as="style">
  <link rel="preload" href="{{ 'js-main-cart.js' | asset_url }}" as="script">
{% endif %}

{% if template.directory == 'customers' %}
  <link rel="preload" href="{{ 'theme-customer.css' | asset_url }}" as="style">
  <link rel="preload" href="{{ 'css-accounts.css' | asset_url }}" as="style">
{% endif %}
