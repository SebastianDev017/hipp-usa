{{- "css-text-image-icons.css" | asset_url | stylesheet_tag -}}

{%- liquid 
	assign image = section.settings.image
	assign title = section.settings.title
	assign text = section.settings.text
	assign button_label = section.settings.button_label
	assign button_link = section.settings.button_link
	assign has_button = false
	
	if button_label != blank and button_link != blank
		assign has_button = true
	endif
-%}

<div class="tii section" data-section-id="{{- section.id -}}" data-section-type="text-image-icons">
  <div class="tii-content section-content">

    <div class="tii-left">
      <div class="tii-image-container">
				<div data-aos="fade-up">
					<div class="tii-image-wrapper square-image">
						{%- unless image == blank -%}
							<img class="tii-image" alt="{{- image.alt -}}" src="{{- image | image_url: width: '900' -}}">
						{%- endunless -%}
					</div>
				</div>

				<div class="tii-icons">
					{%- for block in section.blocks -%}
						{%- liquid 
							assign icon = block.settings.icon
							assign block_text = block.settings.text
						-%}
	
						<div class="tii-icon tii-icon-{{- forloop.index -}}" data-aos="fade-up">
							<div class="tii-icon-image-wrapper square-image">
								{%- unless image == blank -%}
									<img class="tii-icon-image" alt="{{- icon.alt -}}" src="{{- icon | image_url -}}">
								{%- endunless -%}
							</div>
							{%- unless block_text == blank -%}
								<p class="tii-icon-text">{{- block_text -}}</p>
							{%- endunless -%}
						</div>
					{%- endfor -%}
				</div>


      </div>
    </div>

    <div class="tii-right" data-aos="fade-up">
      <div class="tii-info">
        {%- unless title == blank -%}
          <div class="tii-title section-title-richtext color-green">{{- title -}}</div>
        {%- endunless -%}
        {%- unless text == blank -%}
          <p class="tii-text color-green">{{- text -}}</p>
        {%- endunless -%}
        {%- if has_button -%}
          <div class="tii-button-wrapper">
            {%- capture target -%}{%- if button_link contains 'http' or button_link contains 'tel:' or button_link contains 'maps/' or button_link contains 'mailto:' -%}target="_blank"{%- endif -%}{%- endcapture -%}
            <a class="tii-button button" href="{{- button_link -}}" title="{{- button_label -}}" {{ target -}}>{{- button_label -}}</a>
          </div>
        {%- endif -%}
      </div>
    </div>

  </div>
</div>

{%- schema -%}
  {
    "name": "Texto, imagen e íconos",
    "tag": "section",
    "class": "tii-section",
    "settings": [
      {
        "type": "header",
        "content": "Imagen"
      },
      {
        "type": "image_picker",
        "id": "image",
        "label": "Imagen"
      },
      {
        "type": "header",
        "content": "Contenido"
      },
      {
        "type": "richtext",
        "id": "title",
        "label": "Título"
      },
      {
        "type": "text",
        "id": "text",
        "label": "Texto"
      },
      {
        "type": "header",
        "content": "Botón"
      },
      {
        "type": "text",
        "id": "button_label",
        "label": "Texto"
      },
      {
        "type": "url",
        "id": "button_link",
        "label": "Enlace"
      }
    ],
    "max_blocks": 6,
    "blocks": [
      {
        "name": "Ícono",
        "type": "icon",
        "settings": [
          {
            "type": "image_picker",
            "id": "icon",
            "label": "Ícono"
          },
          {
            "type": "text",
            "id": "text",
            "label": "Texto"
          }
        ]
      }
    ],
    "presets": [
      {
        "category": "Homepage",
        "name": "Texto, imagen e íconos"
      }
    ]
  }
{% endschema %}
